<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Cotton.Mobile.Controls"
             xmlns:viewModels="clr-namespace:Cotton.Mobile.ViewModels"
             x:Class="Cotton.Mobile.DiagnosticsPage"
             x:DataType="viewModels:DiagnosticsViewModel"
             Shell.NavBarIsVisible="False"
             Title="Diagnostics">

    <controls:ScreenShellView>
        <controls:TopAppBar PrimaryIconData="{x:Static controls:IconPathData.Copy}"
                            PrimaryCommand="{Binding CopyCommand}"
                            PrimaryDescription="Copy diagnostics"
                            SecondaryIconData="{x:Static controls:IconPathData.Refresh}"
                            SecondaryCommand="{Binding LoadCommand}"
                            SecondaryDescription="Refresh diagnostics" />

        <controls:ScreenScrollBodyView Grid.Row="1">
            <controls:ScreenHeaderView Title="{Binding HeaderTitleText}"
                                       SupportingText="{Binding HeaderVersionText}"
                                       IsBusy="{Binding IsBusy}" />

            <controls:ScreenStatusView Text="{Binding Status}"
                                       IsVisible="{Binding IsStatusVisible}" />

            <controls:StackedItemsView ItemsSource="{Binding Sections}"
                                       StackStyleResourceKey="M3DiagnosticsSectionListStack">
                <controls:StackedItemsView.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:DiagnosticsSectionViewModel">
                        <controls:ContentCardView>
                            <controls:StackedContentView>
                                <controls:SettingsSectionHeaderView Title="{Binding Title}"
                                                                    IsTapEnabled="False" />

                                <controls:StackedItemsView ItemsSource="{Binding Items}"
                                                           StackStyleResourceKey="M3DiagnosticsItemListStack">
                                    <controls:StackedItemsView.ItemTemplate>
                                        <DataTemplate x:DataType="viewModels:DiagnosticsItemViewModel">
                                            <controls:DiagnosticsItemView LabelText="{Binding Label}"
                                                                          ValueText="{Binding Value}" />
                                        </DataTemplate>
                                    </controls:StackedItemsView.ItemTemplate>
                                </controls:StackedItemsView>
                            </controls:StackedContentView>
                        </controls:ContentCardView>
                    </DataTemplate>
                </controls:StackedItemsView.ItemTemplate>
            </controls:StackedItemsView>
        </controls:ScreenScrollBodyView>
    </controls:ScreenShellView>
</ContentPage>
