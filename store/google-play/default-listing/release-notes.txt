Closed testing build for Cotton Cloud mobile.

Adds Notifications settings with permission checks that prompt only after tapping Allow.

Improves queued upload restore after sign-in and running Android transfer notifications with live progress.

Adds file Keep offline and Remove offline actions with On device marker refresh.

Manual uploads, Capture Inbox, Camera Backup setup, local alerts, and Android background transfers remain in testing. True sync is still in progress.
