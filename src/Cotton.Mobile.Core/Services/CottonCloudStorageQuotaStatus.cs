namespace Cotton.Mobile.Services
{
    public enum CottonCloudStorageQuotaStatus
    {
        Unavailable,
        Unknown,
        WithinLimit,
        NearLimit,
        OverLimit,
        Unlimited,
    }
}
