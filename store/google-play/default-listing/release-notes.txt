Closed testing build for Cotton Cloud.

Adds a safe action for replacing a conflicting cloud file with the device copy while preserving version history.

Prevents overlapping scans and duplicate manual runs. Uses native source selection and bottom navigation controls. Removes the empty space and redundant message after media access is granted.
