Closed testing build for Cotton Cloud.

Adds Copy link and Share link for files/folders, using Android clipboard/share sheet flows with offline/error status. Accepts backend plain-text link responses.

Keeps file details, SVG handling, safer file opening, audio/video preview, cached offline start, On device markers, transfer notifications, Storage, feedback, logout cleanup, and Camera Backup queue restore.

Per-link revoke/options, remote push, and true sync remain in progress.
