Closed testing build for Cotton Cloud mobile.

Adds cached offline cold start: after a previous online session, Files can reopen saved folder listings instead of returning to sign-in.

Keeps recent offline folder fixes: On device markers, cached listing age, first-level Up navigation, folder download progress, and Storage budget summaries.

Manual uploads, Capture Inbox, Camera Backup setup, alerts, and background transfers remain in testing. True sync is still in progress.
