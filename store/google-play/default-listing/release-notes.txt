Closed testing build for Cotton Cloud mobile.

Adds a Notifications settings page that checks permission without prompting and asks only after tapping Allow.

Improves queued upload restore after sign-in, so restored waiting transfers can be scheduled again and Files transfer status refreshes after background queue changes.

Manual uploads, Capture Inbox, Camera Backup setup, local transfer alerts, and Android background-transfer foundations remain in testing. True sync is still in progress.
